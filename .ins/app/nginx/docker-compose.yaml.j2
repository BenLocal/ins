services:
  nginx:
    image: nginx:1.26.0
    restart: unless-stopped
    ports:
      - "{{ vars.outbound_port | default(80) }}:{{ vars.port | default(80) }}"
    volumes:
      - "./nginx.conf:/etc/nginx/conf.d/default.conf:ro"
    labels:
      - "com.sample.nginx.port=${INS_SERVICE_A_PORT}"
