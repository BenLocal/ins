services:
  nginx:
    image: nginx:latest
    restart: unless-stopped
    ports:
      - "{{ vars.outbound_port | default(80) }}:{{ vars.port | default(80) }}"
    volumes:
      - "./nginx.conf:/etc/nginx/conf.d/default.conf:ro"
