server {
  listen {{ vars.port | default(80) }};
  listen [::]:{{ vars.port | default(80) }};

  server_name _;

  location / {
    add_header Content-Type text/plain;
    return 200 'ins: {{ app.name | default("nginx") }} is running on port {{ vars.port | default(80) }}';
  }
}
